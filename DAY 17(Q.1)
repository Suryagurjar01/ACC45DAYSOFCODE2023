#include <iostream>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        if(n<=10 && n>=1)
        cout<<"Lower Double"<<endl;
        else if(n<16 && n>=10)
        cout<<"Lower Single"<<endl;
        if(n<26 && n>=16)
        cout<<"Upper Double"<<endl;
        else if(n<=30 && n>=26)
        cout<<"Upper Single"<<endl;
    }
	return 0;
}
